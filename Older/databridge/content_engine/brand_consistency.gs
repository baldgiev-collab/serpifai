function CE_brandConsistency(p){var i=PM_loadProject({project_id:p.project_id||'active'}).inputs||{};var body=String(p.body||'');var issues=[];if(i.forbidden_phrases&&new RegExp(i.forbidden_phrases,'i').test(body))issues.push('Contains forbidden phrases');if(i.voice_guidelines&&!new RegExp(i.voice_guidelines.split(' ')[0]||'','i').test(body))issues.push('Voice not fully aligned');return {ok:true, issues:issues};}
