function CE_internalLinking(p){var targets=String((PM_loadProject({project_id:p.project_id||'active'}).inputs||{}).internal_link_targets||'').split(/[,\n]+/).map(function(x){return x.trim()}).filter(String);var anchors=targets.map(function(t){return {anchor:t, href:'/blog/'+t.replace(/\s+/g,'-').toLowerCase()};});return {links:anchors, suggestion:'Place 2–3 internal anchors within first 60% of the article.'};}
